#include <map>
#include <string>
class UIObject {
 protected:

  // std::string image_path_;
  // std::string audio_path_;
  UIObject();

  virtual ~UIObject() = 0;

 public:
  // UIObject(std::string image_path, std::string audio_path);
};
